// Include the header file that allows input and output operations
#include <iostream>

// Use the standard namespace to avoid writing 'std::' before cin, cout, etc.
using namespace std;

// Main function – execution of the program starts from here
int main() {
    // Print "hello world" to the console followed by a new line
    cout << "hello world" << endl;

    // Return 0 indicates that the program ended successfully
    return 0;
}
